'use strict';

/*

## calcAge Function

### Description
The `calcAge` function calculates the approximate number of days lived based on the given age.

### Syntax
```javascript
function calcAge(age)
 
 */

function calcAge(age) {
    const daysInYear = 365;
    return age * daysInYear;
}
